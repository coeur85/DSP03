class LinkedList2D:
    class Node2D:
        def __init__(self, value, nextStudent, nextCourse):
            self.value = value
            self.NextStudent = nextStudent
            self.NextCourse = nextCourse
